name: Validator
on:
  workflow_dispatch:
jobs:
  ping:
    runs-on: [self-hosted, general-use]
    defaults:
      run:
        shell: bash
    steps:
      - name: Say hello
        run: |
          echo "Validator: parsed YAML and runner picked up the job."