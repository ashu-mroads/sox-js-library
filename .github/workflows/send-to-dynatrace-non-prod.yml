name: Send code to Dynatrace Business Event

on:
  push:
    branches: [ "dev" ]
    paths:
      - "sox-workflow.irn08782.min.mjs"
  workflow_dispatch: {}

jobs:
  ingest:
    runs-on: [self-hosted, general-use]
    defaults:
      run:
        shell: bash

    env:
      DT_ENV: irn08782
      REPO: finance/finance-sox-js-library
      FILE_PATH: sox-workflow.irn08782.min.mjs

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Verify file exists and preview
        run: |
          set -Eeuo pipefail
          echo "==> Checking file: ${FILE_PATH}"
          if [ ! -f "${FILE_PATH}" ]; then
            echo "Missing ${FILE_PATH}"
            exit 1
          fi
          echo "==> Size (bytes):"
          wc -c "${FILE_PATH}" || true
          echo "==> First 10 lines:"
          head -n 10 "${FILE_PATH}" || true

      - name: Ensure jq exists
        run: |
          if ! command -v jq >/dev/null 2>&1; then
            echo "ERROR: 'jq' is required on this runner to build JSON safely."
            echo "Please install jq on the self-hosted runner (Linux: sudo apt-get install -y jq),"
            echo "or ask me for a Python-based variant after jq is installed."
            exit 1
          fi

      - name: Build JSON payload (safe via jq)
        run: |
          set -Eeuo pipefail
          CODE_JSON=$(jq -Rs . < "${FILE_PATH}")
          jq -n \
            --arg provider "github-enterprise" \
            --arg type "com.sox.workflow.updated" \
            --arg repo "${REPO}" \
            --arg file "${FILE_PATH}" \
            --arg sha "${GITHUB_SHA}" \
            --arg run_id "${GITHUB_RUN_ID}" \
            --argjson content "${CODE_JSON}" \
            '{
               "event.provider": $provider,
               "event.type": $type,
               "repo": $repo,
               "file": $file,
               "commit.sha": $sha,
               "github.run_id": $run_id,
               "content": $content
             }' > event.json

          echo "==> Payload size (bytes):"
          wc -c event.json || true
          echo "==> Payload preview (first 500 chars):"
          head -c 500 event.json || true
          echo

      - name: Send to Dynatrace (expect HTTP 202)
        env:
          DYNATRACE_BIZEVENTS_TOKEN: ${{ secrets.DYNATRACE_BIZEVENTS_TOKEN }}
        run: |
          set -Eeuo pipefail
          echo "==> POST https://${DT_ENV}.live.dynatrace.com/api/v2/bizevents/ingest"
          HTTP_STATUS=$(curl -sS \
            -X POST "https://${DT_ENV}.live.dynatrace.com/api/v2/bizevents/ingest" \
            -H "Authorization: Api-Token ${DYNATRACE_BIZEVENTS_TOKEN}" \
            -H "Content-Type: application/json" \
            -D headers.txt \
            --data @event.json \
            -o response.json \
            -w "%{http_code}")

          echo "==> HTTP status: ${HTTP_STATUS}"
          echo "==> Response headers:"
          cat headers.txt || true
          echo "==> Response body:"
          cat response.json || true

          if [ "${HTTP_STATUS}" != "202" ]; then
            echo "ERROR: Dynatrace ingest did not return 202. Failing the job."
            exit 1
          fi

      - name: Success
        run: |
          echo "==> Business Event ingest completed (202 Accepted)."